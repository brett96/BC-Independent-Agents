{
  "name": "bookcover-independent-agents",
  "version": "1.0.0",
  "private": true,
  "description": "BookCover product page and interactive demos for independent insurance agents",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "npx serve dist -p 3000",
    "preview": "npm run build && npm run dev"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "firebase-admin": "^13.4.0",
    "jose": "^6.0.11",
    "nodemailer": "^7.0.5",
    "ua-parser-js": "^2.0.4"
  }
}
